const accountId = 12345;
let email = "pradumkumar0612@gmail.com";
const city = "Jaunpur";
let password = "22ms0095";

// let state;

// Prefer not to use var because of issue in block scope and functional scope

console.table([accountId, email, city, password]);
// if we only create a variable and not assign any value then js code show the undefined in output

// console.table([accountId, email, city, password, state]);

console.log(typeof undefined);
